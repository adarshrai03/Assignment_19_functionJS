(function(){

    // Define the number
    const num = 5;

    // calculate the sqaure
    const square = num*num;

    // Print the Square
    console.log(`The sqaure of the ${num} is ${square}`);
})();